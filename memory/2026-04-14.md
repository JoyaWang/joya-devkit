# 2026-04-14

## Laicai release 接入状态审计
- 结论：Laicai 的 release 目前还没有真正切到 `shared-runtime-services` 作为上传签名、release 登记和公共分发真相源；当前仍主要走 Laicai 自己的 CloudBase storage 函数 + `OPS_RELEASE_REGISTER_URL` 链路。
- 关键证据：`Laicai/.github/workflows/app-release.yml` 仍调用 `OPS_RELEASE_REGISTER_URL`；`Laicai/backend/cloudbase/functions/storage/index.js` 仍处理 `POST /release/upload-url`；`shared-runtime-services/steering/BACKEND_STRUCTURE.md` 明确写着 Laicai 仍是项目自有 dev/prd bucket、尚未完成共享两桶物理收口。
- 补充判断：`shared-runtime-services` 已有 `/v1/releases` 与 `public-delivery` 路由，也已有 shared prefix 的线上验证，但这说明共享服务能力在收口，不等于 Laicai 已完成切流。
