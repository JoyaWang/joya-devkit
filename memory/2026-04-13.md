# 2026-04-13

## joya-ai-sys 合规审计
- 对 `shared-runtime-services` 做了项目级 joya-ai-sys 合规审计。
- 结论：结构层大体合规，但尚未达到完整合规基线。
- 关键问题：1) Git 基线不合规（缺 `dev` 分支，当前开发落在 `main`）；2) `IDENTITY.md` 与 `SOUL.md` 为空；3) `steering/SESSION_CONTEXT.md` 当前切片与下一步动作口径冲突；4) `HERMES.md`、`IDENTITY.md`、`SOUL.md`、`MEMORY.md` 与 `memory/` 仍是未纳入版本控制的本地状态，仓库基线未完全固化；5) `TEST_PLAN.md` 与当前实际测试工具/命令存在陈旧漂移。

## 合规修复收口
- 已按“只做本地、不推送”的约束完成基线修复。
- Git 基线已收口到本地 `dev` 分支；未执行 push / commit。
- `scripts/seed-projects-config.ts` 已按 TDD 修复 shared non-prod 默认 `downloadDomain` 回落逻辑；`tests/seed-projects-config.test.mts` 已转绿。
- 项目身份层已补齐：`IDENTITY.md` 改为项目身份而非 agent 自我介绍，`SOUL.md` 写明 TDD、文档先行与 autonomous mode 行为准则。
- `.agent/runtime/` 已初始化，`execution-state.json` / `slice-packets` / `checkpoints` / `evidence` / `reconcile-log.jsonl` 已形成最小 long-running 恢复链。
- `steering/SESSION_CONTEXT.md` 已从 Laicai 接入口径切回 `shared-runtime-services` 自身的 runtime baseline；`steering/IMPLEMENTATION_PLAN.md` 补入 Phase/Slice 与 Existing Project Onboarding framing；`steering/TEST_PLAN.md` 已对齐真实 Vitest / typecheck / E2E 命令。
- 验证结果：`pnpm exec vitest run` 19 个测试文件 / 138 个测试全通过；`pnpm exec tsc --noEmit --project tsconfig.json --pretty false` 通过。
- 经验：项目 `IDENTITY.md` 必须表达“项目是谁”，不能误写成执行该任务的 agent 身份；`SESSION_CONTEXT.md` 与 `.agent/runtime/*` 必须一起收尾，否则 autonomous 恢复会出现口径漂移。
