# 2026-04-11

## shared-runtime-services 会话存档
- 用户明确要求：先给 Laicai 接入方案，不动代码；将在另一个会话继续当前任务。
- 本轮已完成结论：Laicai 可以作为 `shared-runtime-services` 第一条真实接入链路，InfoV 继续作为第二优先对象。
- 当前推荐接法：独立分支接入、先 dev 后 prd、先写侧后读侧、保留旧 backend/workflow 与 legacy 下载链路作为 fallback，验证通过后再合并，目标是不影响 App 可用性。
- 若在新会话继续，优先读取 `steering/SESSION_CONTEXT.md`，再读取 Laicai 的 `.github/workflows/app-release.yml`、`backend/cloudbase/functions/storage/cos_storage_provider.js`、`.env.dev.example`、`.env.prod.example`、`ENVIRONMENT_SWITCHING.md`、`docs/dl.infinex.cn-cdn-android-release-setup.md`。
